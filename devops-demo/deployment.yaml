apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring-boot-deployment
  namespace: namespace-1
  labels:
    app: webapp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: webapp
  template:
    metadata:
      labels:
        app: webapp
    spec:
      containers:
      - name: web-container
        image: host.docker.internal:80/demo/devops-demo:0326b9d
        ports:
        - containerPort: 8080
      imagePullSecrets:
      - name: harbor-secret